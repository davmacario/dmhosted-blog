{
  "name": "dmhosted-blog",
  "version": "0.0.0-managed-by-semantic-release",
  "private": true,
  "description": "Release tooling for blog.dmhosted.com (the site itself is built with Hugo)",
  "license": "MIT",
  "scripts": {
    "release": "semantic-release",
    "release:dry": "semantic-release --dry-run --no-ci"
  },
  "devDependencies": {
    "@semantic-release/exec": "^7.1.0",
    "semantic-release": "^24.2.3"
  },
  "engines": {
    "node": ">=22.23.2"
  }
}
